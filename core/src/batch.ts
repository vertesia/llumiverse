/**
 * Batch inference support types and interfaces
 */

import type { 
  BatchInferenceJob, 
  BatchInferenceJobStatus, 
  BatchInferenceResult, 
  BatchItemResult 
} from './types.js';

// Re-export the batch inference types
export type { 
  BatchInferenceJob, 
  BatchInferenceJobStatus, 
  BatchInferenceResult, 
  BatchItemResult 
};